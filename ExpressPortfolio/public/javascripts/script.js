// Check if the element exists before trying to interact with it
const projectsElement = document.getElementById('projects');
if (projectsElement) {
  projectsElement.scrollIntoView(); // Only run if the element is found
}

const contactElement = document.getElementById('contact');
if (contactElement) {
  contactElement.scrollIntoView(); // Only run if the element is found
}

const servicesElement = document.getElementById('services');
if (servicesElement) {
  servicesElement.scrollIntoView(); // Only run if the element is found
}

const aboutElement = document.getElementById('about');
if (aboutElement) {
  aboutElement.scrollIntoView(); // Only run if the element is found
}
